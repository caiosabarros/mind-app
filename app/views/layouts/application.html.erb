<!DOCTYPE html>
<html>
  <head>
    <title>MindApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
      
    <% if user_signed_in? %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <%= link_to "LogoutBro", destroy_user_session_path, method: :delete, class:'bg-primary badge text-wrap' %>
        <%= link_to "Contents", contents_path, class:'bg-primary badge text-wrap' %>
        <%= link_to "Home", home_path, class:'bg-primary badge text-wrap' %>
      <% end %>    
      </nav>
      <%= yield %>
    </div>
  </body>
</html>
